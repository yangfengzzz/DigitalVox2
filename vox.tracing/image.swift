//
//  image.swift
//  vox.tracing
//
//  Created by 杨丰 on 2021/9/6.
//

import Foundation


// -----------------------------------------------------------------------------
//MARK:- EXAMPLE IMAGES
// -----------------------------------------------------------------------------
// Convert a bump map to a normal map. All linear color spaces.
func bump_to_normal(image: image_data, scale: Float = 1) -> image_data {
    fatalError()
}

// Add a border to an image
func add_border(img: image_data, width: Float, color: vec4f = [0, 0, 0, 1]) -> image_data {
    fatalError()
}
